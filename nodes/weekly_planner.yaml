id: weekly_planner
version: 1.0.0
type: deterministic
enabled: true
instructions: Distribuye discovery, deepening, application, comment y review antes de cada reunión.
rules: [timezone_aware, no_overdue_material]
input_schema: {type: object}
output_schema: {type: object}
allowed_tools: []
guardrails: [deadline_integrity]
evaluation_suite: [week_schedule_fixture]
